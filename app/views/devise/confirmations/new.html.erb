<section class="welcome-section relative min-h-screen">
  <div class="relative z-10 flex flex-col items-center justify-center h-full px-6 py-12 max-w-7xl mx-auto">
    <div class="w-full max-w-md p-8 bg-white bg-opacity-90 rounded-xl shadow-lg backdrop-blur">
      <h2 class="text-2xl font-bold text-gray-800 mb-6">Reenviar instrucciones de confirmación</h2>

      <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: "space-y-4" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, "Correo electrónico", class: "block text-sm font-medium text-gray-700" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class: "mt-1 block w-full rounded-lg border-gray-300 shadow-sm focus:border-pink-500 focus:ring-pink-500 sm:text-sm" %>
        </div>

        <div>
          <%= f.submit "Reenviar instrucciones de confirmación", class: "w-full bg-pink-600 hover:bg-pink-700 text-white font-semibold py-2.5 px-4 rounded-lg shadow" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
      <%= link_to "Volver atrás", :back, class: "block text-sm text-pink-600 hover:underline font-medium mt-4" %>
    </div>
  </div>
</section>