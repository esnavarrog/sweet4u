<div class="flex h-full">
  <div class="w-1/3 bg-white border-r border-gray-200 flex flex-col mt-16 hidden md:block">
    <div class="flex justify-center items-center border-b border-gray-200 h-[81px]"><h1 class="text-2xl font-bold">Tus Conversaciones</h1></div>


    <div class="flex-grow space-y-2 p-2 overflow-y-auto">
      <% @conversations.each do |conversation| %>
        <%= render partial: 'conversations/conversation_list_item',
                   locals: { conversation: conversation, current_user: current_user } %>
      <% end %>
    </div>

  </div>

  <div class="w-full md:w-2/3 bg-gray-50 flex flex-col mt-16 max-h-full">
    <%= turbo_frame_tag "chat_panel" do %>
      <div class="flex-grow flex items-center justify-center text-gray-500 text-lg chat-messages-container">
        Selecciona una conversación para empezar a chatear.
      </div>
    <% end %>
  </div>
</div>